from graphics_tk import make_fretboard
from chord_dicts import chords_to_intervals, note_to_index


def draw_chord(
        chord_root: str,
        chord: str,
        instrument_str_semitones_from_c: list[int],
        fret_x_coord_midpoints: list[float],
        string_y_coords: list[float],
) -> None:
    return


if __name__ == '__main__':
    pass
